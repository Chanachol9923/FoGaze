opencv-python>=4.8.0
mediapipe>=0.10.0,<0.10.10  # pin to avoid API migration to mp.tasks
numpy>=1.24.0
ultralytics>=8.0.0
pyttsx3>=2.90
Pillow>=10.0.0
# Uncomment the line below for real OCR support:
# pytesseract>=0.3.10
